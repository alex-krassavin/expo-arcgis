---
title: Display a map
description: A basemap and an initial viewpoint.
---

import { Code } from '@astrojs/starlight/components';
import source from '../../../../../example/app/basics/display-map.tsx?raw';

Displays a topographic basemap centered on the Santa Monica Mountains. The `<Map>` model holds the
basemap and initial viewpoint; the `<MapView>` host renders it.

<Code code={source} lang="tsx" title="app/basics/display-map.tsx" />

This is the real source of the [example app](https://github.com/alex-krassavin/expo-arcgis/tree/main/example)
sample — run it on a device or simulator to see it live.
