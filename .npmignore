# What ships is controlled by the `files` allowlist in package.json. This file only carves
# native build *outputs* (Gradle / Xcode) out of the published android/ and ios/ source trees.

android/build
android/.cxx
ios/build

# Tarballs produced by `npm pack`
*.tgz
